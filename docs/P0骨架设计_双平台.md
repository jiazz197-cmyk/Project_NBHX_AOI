# P0 骨架设计：双平台 monorepo（D1~D3 落实依据）

> 定位：单仓库、**两个独立可部署体**——平台 A（LS 1.x 二开，中心侧）+ 平台 B（FastAPI + 独立 React 前端，产线侧），唯一共享层为 `packages/skillname` 与 `packages/pipeline-core`。
> 依据：`docs/双平台架构与拆分方案.md`、`docs/contracts/`（D2 冻结）、`docs/MVP开发计划.md`。
> 只定义骨架（结构/职责/签名/stub 行为/验收口径），代码自 D1 起落实。归属：🟦B · 🟧C · 🟪共担 · ⬜A。

---

## 1. 整体仓库骨架

> 目录与**当前仓库实际结构对齐**：LS 源码在 `label_studio/`，LS 前端在 `web/`；二开后端集中在 `label_studio/aoi/`；平台 B 全部新增在 `infer-platform/`。

```
Project_NBHX_AOI/                      # 交付仓库（monorepo，两个独立可部署体）
├── label_studio/                      # 🟦 平台A 后端：LS 1.x fork（上游模块只读，红线见 §2.3）
│   ├── core/ users/ projects/ tasks/ data_export/ data_manager/ ml/ ...   # 上游，只读
│   ├── aoi/                           # 🟦 二开唯一可写区
│   │   ├── urls.py                    #   汇总二开路由于 core/urls.py 一行注入
│   │   ├── core/                      #   自研 RBAC（角色/权限点/授权表、DRF 权限类）、request_id、错误码、信封
│   │   ├── datasets/                  #   字典/label config/导入包裹/数据集版本/划分红线
│   │   ├── prelabel/                  #   预标任务 + LS ML backend 协议（A 自实现）
│   │   ├── training/                  #   基模/训练任务/门禁/模型注册/审批/模型发布（构建镜像+push）
│   │   ├── review/                    #   错图接收/复审工作项/终裁/建议清单/坏图
│   │   ├── audit/                     #   关键操作审计（aoi_audit.audit_log）
│   │   ├── reports/                   #   数据/训练侧报告（最后做，可砍）
│   │   └── workers/                   #   Celery（training=GPU / default=CPU / publish=发布镜像）
│   └── manage.py
├── web/                               # 🟦 平台A 前端：LS 1.x web（React + TS）
│   └── apps/labelstudio/src/
│       ├── pages/{Datasets,Training,Review,System}/         # 二开页面（独立路由）
│       └── components/Menubar/                              # 菜单挂载点（上游改动最小化）
├── infer-platform/                    # 🟧 平台B：独立前后端，独立发版/部署
│   ├── backend/
│   │   ├── pyproject.toml             #   fastapi/uvicorn/sqlalchemy/onnxruntime/apscheduler/jinja2
│   │   └── app/
│   │       ├── main.py                #   装配 + StaticFiles 托管前端构建产物
│   │       ├── config.py              #   环境变量契约（§4.5）
│   │       ├── db.py                  #   SQLite(WAL) + 顺序迁移执行器
│   │       ├── envelope.py            #   统一信封 + 错误码（平台B契约 §1.3）
│   │       ├── deps.py                #   无用户认证；仅 registry 凭据与 outbox 透传
│   │       ├── api/                   #   health/inspect/records/stats/reports/models/
│   │       │                          #   stations/templates/system
│   │       ├── registry/              #   pull.py（Registry v2 拉取）model_yaml.py（能力描述校验）
│   │       ├── inference/             #   runtime.py(OnnxRuntimeModel) engine.py
│   │       ├── camera/                #   base.py directory.py gige.py
│   │       ├── store/                 #   models.py(ORM) migrations/ retention.py
│   │       ├── outbox/                #   queue.py client.py（回传 A + 重试）
│   │       ├── report/                #   daily.py templates/daily.html.j2
│   │       └── scheduler.py           #   APScheduler 任务注册
│   ├── frontend/                      #   Vite + React + TS + Ant Design 5 + ECharts
│   │   └── src/{api,pages,components}/ #   pages: Dashboard/Inspections/Reports/Stations/System
│   └── deploy/                        #   Dockerfile / compose / systemd / 离线包脚本
├── packages/                          # 🟪 唯一共享层（纯 Python，框架无关）
│   ├── skillname/                     # 🟦 词汇表：任务类型/code/model_ref/镜像 tag 规范
│   └── pipeline-core/                 # 🟧 推理内核：切片/NMS/合并/三档判定/load_config
├── tests/contracts/                   # 🟪 双端契约测试 + fixtures
├── deploy/                            # ⬜ 平台A 部署（nginx/uwsgi/compose/pg/minio/redis）
└── docs/                              # 架构/计划/骨架/契约
```

**包管理**：`packages/*` 各自独立 `pyproject.toml`，A/B 通过 path/editable 依赖安装（`uv pip install -e packages/skillname -e packages/pipeline-core`）；不强制 uv workspace（如需可用根 `uv.toml` 配置，不污染 LS 的 `pyproject.toml`）。

---

## 2. 平台 A 骨架（🟦）

### 2.1 二开 app 结构

```
label_studio/aoi/
├── __init__.py
├── apps.py            # 每个 app 一个 AppConfig；INSTALLED_APPS 追加 aoi.*
├── urls.py            # /api/* 二开路由此汇总
├── common/            # 信封、错误码、分页、幂等键、权限基类、审计装饰器
├── core/              # 自研 RBAC：角色/权限点/授权表、DRF 权限类、/api/core/permissions
├── datasets/          # 字典/label config/导入包裹/数据集/版本/划分红线/导出包裹
├── prelabel/          # 预标任务 + ML backend 协议端点 + 三桶路由
├── training/          # base_model/train_job/model/preset + 门禁 + ONNX + 审批 + 模型发布
├── review/            # /api/ingest/findings + workitem/finalize/suggestions/bad-images
├── audit/             # 关键操作审计
└── reports/           # 数据/训练侧统计（占位）
```

### 2.2 上游注入点（允许改动，全部记 `CHANGES.md`）

| 文件 | 改动 | 说明 |
|---|---|---|
| `label_studio/core/settings/base.py` | `INSTALLED_APPS += ['aoi.core', 'aoi.datasets', ...]` | 单行区段，附注释 |
| `label_studio/core/urls.py` | `re_path(r'^', include('aoi.urls'))` | 一行，挂载 `/api/*` 二开路由于 `/api/ingest/*` |
| `web/apps/labelstudio/src/components/Menubar/` | 新增「数据集/训练/复审/系统」入口 | 素材级 + 菜单项，不改上游组件逻辑 |
| `web/apps/labelstudio/src/pages/index.js` | 注册二开页面路由 | 一行一页 |

> 除上述注入点外，上游文件**不得写业务代码**；所有二开逻辑在 `label_studio/aoi/` 与 `web/apps/labelstudio/src/pages/` 内。

### 2.3 二开硬底线

1. 上游模块（`core/users/projects/tasks/data_export/data_manager/ml/...`）只读；改动仅限 §2.2 注入点，且可 diff 上游。
2. 标注编辑器 / Data Manager 零重构。
3. 不动 LS 原生表语义；aoi 独立建表（`aoi_*` schema）。
4. 前端二开页面独立路由，不侵入原生组件树。
5. LS 自带 `django_rq` 仅服务 LS 原生功能；**aoi 二开异步任务统一 Celery**，两者共存。

### 2.4 训练域四表前置（D1 必须就位）

`aoi_training.train_job` / `base_model` / `model`（含 `task_type`、`cover_classes`、`weights_key`、`lifecycle`）/ `preset`；`task_type` 枚举由 `skillname` 裁定（`ObjectDetection`），**不依赖 `ml_models.SkillNames`**（上游只有 TextClassification/NER）。

### 2.5 新增表（P0 建迁移，D4 起使用）

```sql
-- 模型发布状态（A→镜像仓库）
aoi_training.model_publish(
  id, model_ref UNIQUE, registry, image, tag, digest,
  status,                                  -- queued/building/pushing/published/failed
  attempts INT, error_message TEXT,
  published_by INT, created_at, published_at)
```

> **自研 RBAC 四表**（`aoi_core.role` / `permission` / `role_permission` / `user_role`）随 D4 RBAC 迁移创建，
> 不依赖 LS 原生角色/组织权限框架（该框架不可用），详见平台 A 契约 §3.1。

### 2.6 A 侧端点 stub 行为（D3 验收口径）

| 端点 | stub 行为 | 关键错误 |
|---|---|---|
| `GET /api/core/permissions` | 返回当前登录用户 `{user_id, roles:[...], perms:[...]}`（供 A 前端按钮显隐；权限来自 `aoi_core` 自研 RBAC） | LS JWT |
| `POST /api/ingest/findings` | 幂等校验 `(station_code, seq)` → 落图片；`suspicious` → fact+workitem，`bad` → bad_image → `{fact_id, workitem_id, image_id, bad_image_id, duplicated}` | 40100/40900/42200 |
| `POST /api/train/models/{id}/publish` | 写 `model_publish=queued` → Celery 构建镜像并 `docker push` → 返回 `{publish_id}` | 40100/40401/40900/50300 |
| `GET /api/train/models/{id}/publish` | 发布状态（stub 返回 `queued`） | 40401 |
| `GET /api/train/jobs/{id}/progress` | SSE stub：training×2~3 + finished | — |
| `POST /api/prelabel/{task_id}/{health,setup,predict,validate}` | LS 官方协议 stub；predict 用 `StubRuntimeModel` 假框 | 无内部头/无令牌 → 40100 |

---

## 3. 平台 B 骨架（🟧）

### 3.1 后端目录（§1 已列），关键约定

- **单进程**：Uvicorn 1 worker；推理用进程内线程池（`INSPECT_CONCURRENCY=2`），有界队列（`INSPECT_QUEUE_MAX=32`）满则 42900。
- **SQLite**：`PRAGMA journal_mode=WAL; synchronous=NORMAL; busy_timeout=5000`；写操作串行化（单写锁），统计聚合按分钟批量写。
- **前端托管**：`app.mount('/assets', StaticFiles(...))` + SPA fallback 到 `index.html`；开发期 Vite 代理 `/api` 到 `:8990`。
- **无 RBAC / 无用户认证**：B 不做登录与用户体系，所有接口直接放行；`deps.py` 只注入 request_id 与 registry 只读凭据（拉模型用）。

### 3.2 B 侧数据表（SQLite，`b_` 前缀；见平台 B 契约 §2）

`b_model` / `b_station`（含 `template_json` 工位模板）/ `b_defect_class` / `b_inspection` / `b_bad_image` / `b_outbox` / `b_stats_daily` / `b_report` / `b_setting` / `b_audit`（无 `b_plan`）。

### 3.3 B 侧端点 stub 行为（D3 验收口径）

| 端点 | stub 行为 | 关键错误 |
|---|---|---|
| `GET /api/v1/health` | 服务/GPU/磁盘/模型/工位/outbox 结构齐备 | 开放 |
| `POST /api/v1/inspect/image` | multipart → 解码（坏图 → 40010 + 登记）→ 无工位模板 → 42200 → `pipeline-core.run(StubRuntimeModel)` → 落库 → 响应 | 40010/40402/42200/42900/50300 |
| `POST /api/v1/inspect/batch` | `{object_keys, station_code}` 逐个 stub → 汇总 | 同上 |
| `GET /api/v1/inspections` `/inspections/{id}` `/image` | 列表/详情/图（stub 返回空列表与占位图） | 40401 |
| `GET /api/v1/stats/summary` `/stats/trend` `/stats/errors` | 返回空结构（字段齐备） | 42200 |
| `GET /api/v1/reports/daily` `/reports/daily/{day}` | 列表/详情（stub 空） | 40401 |
| `GET /api/v1/models` `/stations` | 列表 | — |
| `POST /api/v1/models/pull` | 校验 image 格式 → 返回 `{status:"pulling"}`（D3 用假 manifest 走通流程） | 40010/42200/50300 |
| `POST /api/v1/models/import` | multipart tar → 解包校验（stub） | 40010/42200 |
| `GET/PUT /api/v1/stations/{code}/template` | 工位模板读写（stub 返回示例模板；PUT 用 `pipeline-core.load_config` 校验） | 42200/40401 |
| `GET /api/v1/system/outbox` | 回传队列状态 | — |

### 3.4 B 侧前端骨架（A 主笔）

```
infer-platform/frontend/src/
├── main.tsx / App.tsx / router.tsx
├── api/client.ts        # fetch 封装：信封解包 + 错误提示
├── pages/
│   ├── Dashboard/       # 概览：今日检测量/三档分布/缺陷TopN/节拍/时延/在线状态
│   ├── Inspections/     # 检测记录：列表+筛选+图片/框预览（含错图 tab）
│   ├── Reports/         # 日报：列表/详情/CSV 导出
│   ├── Stations/        # 工位与相机：工位增删改/相机配置/软触发/快照 + 工位模板编辑
│   └── System/          # 模型库（拉取/导入/删除）/健康/outbox/保留策略
├── components/          # StatCard/ChartCard/StatusTag/ImagePreview
└── styles/
```

约束：不复用 `web/` 的 LS 组件；首屏 gzip ≤ 2MB；不做视频流与大屏。

---

## 4. 公共包骨架（🟪）

### 4.1 `packages/skillname`（🟦 主笔）

```
skillname/
├── pyproject.toml            # 零依赖；requires-python >=3.10
└── skillname/
    ├── __init__.py           # 导出全部公共符号
    ├── enums.py              # SkillName / ModelLifecycle / DatasetSubset / Verdict
    ├── codes.py              # is_valid_fault_code / 颜色与索引约定
    └── model_ref.py          # ModelRef / format_model_ref / parse_model_ref / image_tag_from_model_ref
```

验收：`python -c "import skillname"` 无三方依赖；`ObjectDetection → RectangleLabels` 映射与 LS 实测一致（D2 复用验证日）。

### 4.2 `packages/pipeline-core`（🟧 主笔）

```
pipeline-core/
├── pyproject.toml            # numpy + pillow；[yaml] extra: pyyaml
└── pipeline_core/
    ├── types.py              # DetectBox / DetectResult / ObjectSpec / InspectConfig / Tile / RawBox
    ├── runtime.py            # RuntimeModel(ABC)
    ├── tiling.py             # slice_image / box_to_global
    ├── merge.py              # merge_across_tiles(NMS)
    ├── verdict.py            # decide_verdict
    ├── config.py             # load_config
    ├── pipeline.py           # run
    └── stub.py               # StubRuntimeModel
```

签名（与架构文档 §5.2、平台 B 契约 §4 一致）：

```python
@dataclass(frozen=True)
class DetectBox:   xyxy: tuple[float, float, float, float]; class_id: int; object_code: str; score: float; model_ref: str = ""
@dataclass
class DetectResult: boxes: list[DetectBox]; tiling_meta: dict; verdict: str; verdict_reasons: list[str]
@dataclass
class ObjectSpec:   code: str; model_ref: str; class_map: dict[int, str]; recheck_min: float; auto_min: float; risk_level: int
@dataclass
class InspectConfig: objects: list[ObjectSpec]; tile_size: int = 1280; overlap: float = 0.2

class RuntimeModel(ABC):    def infer(self, tiles: list[np.ndarray]) -> list[list[RawBox]]: ...

def slice_image(img, tile_size=1280, overlap=0.2) -> list[Tile]
def box_to_global(box, offset) -> DetectBox
def merge_across_tiles(boxes, iou_thr=0.5) -> list[DetectBox]
def decide_verdict(boxes, cfg) -> tuple[str, list[str]]
def run(image, cfg, models: Mapping[str, RuntimeModel]) -> DetectResult
def load_config(yaml_text: str) -> InspectConfig
class StubRuntimeModel(RuntimeModel)
```

**调用方适配器**（不进公共包）：
- A：`aoi/prelabel/runtime.py::OnnxRuntimeModel`（Celery GPU worker 内）；
- B：`infer-platform/backend/app/inference/runtime.py::OnnxRuntimeModel`（FP32/FP16、CUDA/CPU）。

### 4.3 契约测试（🟪）

```
tests/contracts/
├── test_skillname.py            # 枚举/code/model_ref/镜像 tag 规范
├── test_pipeline_core.py        # 切片/合并/三档判定/load_config/StubRuntimeModel
├── test_cross_platform.py       # 镜像 manifest/digest 校验；model.yaml 校验；回传幂等
└── fixtures/
    ├── detect_result_sample.json
    ├── model_yaml_sample.yaml     # 含必填/可选/预留字段样例
    ├── model_manifest_sample.json
    ├── inspect_config_sample.yaml
    └── findings_ingest_sample.json
```

消费方写用例、维护方 CI 跑；D2 起挂 CI，红则阻断合并。

---

## 5. D1~D3 落实步骤

**仓库线（⬜A 主笔）**
- D1：monorepo 目录 + `packages/` 两包骨架 + CI 骨架（lint/test/契约测试）+ 双平台端口与网络规划。
- D2：A 侧 compose（nginx/ls-backend/workers/pg/minio/redis）+ B 侧 compose/systemd（backend/frontend 构建）+ 跨机连通性脚本 + 离线依赖清单。
- D3：契约测试挂 CI；B 前端脚手架与 mock。

**平台 A 线（🟦B 主笔）**
- D1：`label_studio/aoi/` app 骨架 + 注入点 + `aoi_training` 四表迁移 + `skillname` 枚举裁定 + `aoi/urls.py` 挂载。
- D2 上午：共享表迁移 + label config 生成 stub + aoi API 全量 stub（`/api/datasets|train|review|system` + `/api/ingest/*`）。
- D2 下午：**复用验证日**（LS 8 项实测，输出复用结论表；不符项当场定降级方案并回写契约）。
- D3：JWT 登录链确认 + `/api/ingest/findings` 打桩应答 + `model_publish` 表 + 发布服务 stub（假 build/push）。

**平台 B 线（🟧C 主笔）**
- D1：FastAPI + SQLite + APScheduler + 信封/错误码 + `deps.py` + `pipeline-core` 真逻辑（切片/合并/判定/load_config）+ `StubRuntimeModel`。
- D2：端点全量 stub + fixtures + 模型拉取/工位模板 stub + outbox 骨架 + 前端壳（5 页路由）+ 契约测试跑绿。
- D3：registry 假 manifest 拉取链路 + 工位模板校验链路 + 与 A 打桩联调（错图回传）+ B 前端对接 mock。

**D3 空跑验收（10 项）**

| # | 项 | 归属 |
|---|---|---|
| 1 | LS 登录/令牌可用 | A/B |
| 2 | 新建项目可注入 label config（`object_fault_type_XX`） | B |
| 3 | 上传图片 + 缩略图 + 预签名下载正常 | B |
| 4 | 框标注 + Review 流走通 | B |
| 5 | data_export YOLO 导出 smoke 通过 | B |
| 6 | A 侧 `/api/*` stub 全量 200 + OpenAPI 可导出 | B |
| 7 | B 侧 `/api/v1/*` stub 全量 200 + OpenAPI 可导出；`/health` 信封正确 | C |
| 8 | **模型发布/拉取 stub 全绿**（`model.yaml` 生成 + 假 manifest 拉取 + sha256/digest 校验 + 幂等） | B/C |
| 9 | **B→A 错图回传 stub 全绿**（幂等键 + 重复请求 `duplicated=true` + 坏图无图分支） | B/C |
| 10 | `skillname` / `pipeline-core` 双端契约测试绿 | B/C |

**并行依赖输入**：B——LS tag 锁定与离线构建清单（给 A）、`skillname` 初版、fixture 3 份（`model_yaml_sample.yaml`/`model_manifest_sample.json`/`ml_backend_predict_sample.json`）、JWT 定义、aoi 四表迁移（给 C 联调）；A——离线 yarn/pip 源、registry 账号与镜像仓库地址、CI 挂载；C——`pipeline-core` 签名评审（B 确认预标侧可用）、fixture 2 份（`findings_ingest_sample.json`/`inspect_config_sample.yaml`）、B 侧依赖清单（给 A 打包）。

---

## 6. 配置键清单（D2 冻结）

**平台 A**（`.env` 追加）：`MODEL_REGISTRY=docker.io`、`MODEL_IMAGE_REPO=<org>/aoi-model`、`MODEL_REGISTRY_USER`、`MODEL_REGISTRY_PASSWORD`、`INTERNAL_TOKEN`、`PUBLISH_RETRY=3`。

**平台 B**（`infer-platform/.env`）：`PORT=8990`、`TZ=Asia/Shanghai`、`INSTANCE_CODE=B01`、`DB_PATH=/data/infer.db`、`DATA_DIR=/data`、`MODEL_REGISTRY=docker.io`、`MODEL_REGISTRY_USER`、`MODEL_REGISTRY_TOKEN`（只读，可空）、`MODEL_PULL_MODE=oci|docker`、`TRAIN_PLATFORM_BASE_URL`、`INTERNAL_TOKEN`、`INSPECT_QUEUE_MAX=32`、`INSPECT_CONCURRENCY=2`、`INFER_TIMEOUT_MS=10000`、`ONNX_PROVIDER=cuda|cpu`、`AUTO_PASS_KEEP_IMAGE=false`、`AUTO_PASS_KEEP_THUMB=true`、`IMAGE_RETENTION_DAYS=30`、`MODEL_KEEP_VERSIONS=3`、`REPORT_RETENTION_DAYS=90`、`REPORT_CRON="10 0 * * *"`、`OUTBOX_MAX_AGE_HOURS=24`、`PULL_TMP_TTL_HOURS=24`、`LOG_LEVEL=INFO`。

---

*本文为 P0 骨架设计；D2 契约冻结前，签名以本文 + `docs/contracts/` 为准；复用验证日的实测结论高于纸面推断。*
